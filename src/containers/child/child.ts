import { Component } from '@angular/core';

@Component({
  selector: 'Child',
  templateUrl: './child.html',
  styleUrls: ['./child.scss'],
})
export class Child {
  public pageTitle = 'Child Page';
}
